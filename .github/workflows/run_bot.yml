name: Crypto Exchange Listing Bot

on:
  schedule:
    # این دستور یعنی ربات هر ۵ دقیقه یک‌بار به طور خودکار اجرا بشه
    - cron: '*/5 * * * *'
  workflow_dispatch: # این دکمه رو می‌ذاره تا خودت هم بتونی دستی اجراش کنی

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Bot Script
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python bot.py

    - name: Commit and Push DB Changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add sent_listings_tw_ai.txt || true
        git commit -m "Update sent listings database [skip ci]" || true
        git push || true
